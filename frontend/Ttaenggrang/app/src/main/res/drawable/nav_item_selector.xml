<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 선택된 상태 (파란색 & 둥근 모양) -->
    <item android:drawable="@drawable/nav_selected_bg" android:state_checked="true"/>
    <!-- 기본 상태 (흰색 배경) -->
    <item android:drawable="@color/white"/>

</selector>